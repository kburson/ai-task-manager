# Review Evidence for #1268

This directory indexes accepted co-review evidence for issue #1268. Artifact type
is structural rather than encoded ambiguously into otherwise identical filenames:

- [`spec/`](./spec/) contains review evidence for the normative design.
- [`plan/`](./plan/) contains review evidence for the executable implementation
  plan.

This issue follows the repository-wide
[review-evidence layout and naming standard](../README.md). Each artifact directory
owns its authority commit, protocol ID, terminal decision, source and
repository-copy hashes, and runtime provenance.
