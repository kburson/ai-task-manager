# Proposed Codex adapter (measurement fixture, not installed)

Read `skill/shared/router.md`, pickup, and the JIT rule for the verb. Current-context `id@digest` and source receipts are independent. Discard both after compaction, clear, or a fresh task. Follow the live issue's assigned role and exact worktree.

For delivery, only exit 20 with exactly one `AITM_PROVIDER_ACTION_REQUIRED:` JSON line permits one matching provider action. [delivery.envelope] Validate the complete envelope, expected head, adapter capability, and merge method. Never invoke shell merge, `gh pr merge`, or a hidden subprocess. [delivery.no-shell] After any outcome, rerun `npx aitm deliver <N>` to independently verify live GitHub and trunk state; do not repeat the provider mutation before reconciliation. [delivery.reconcile] Close only with a live-verified receipt.
