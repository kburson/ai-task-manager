# Cost Fixtures

Fixtures in `scripts/tests/helpers/cost/fixtures.mjs` are public-safe examples
for story-level agent and automation cost records. They use fixed identifiers,
fixed timestamps, no prompt/tool-body content, and no credential-shaped keys.
