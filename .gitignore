node_modules/

*.tgz
.DS_Store

output/
reports/
.tmp/
tmp/

# c8 code-coverage output (#570) — regenerable from `npm run test:coverage`
coverage/

# ai-task-manager — user configuration files (do not commit)
.ai-task-manager/pickup-directive.md.bak
.ai-task-manager/definition-of-done.md.bak
# pickup-directive.md + definition-of-done.md are skill-installed but
# team-editable shared templates (like task-tracker.json) — tracked in git so
# fresh worktrees and the Test gate receive them with no seeding step (#569).
# Install-output copies of templates/ (regenerable; templates/ is the source)
.ai-task-manager/session-boot.md
.ai-task-manager/session-state-template.md
.ai-task-manager/worker-report.md
.ai-task-manager/epic-body.md
.ai-task-manager/solo-issue-body.md
.ai-task-manager/sub-issue-body.md
.ai-task-manager/references/
# project-fields.json + project-field-events.json are stable declarative board
# config (schema, aliases, per-transition field-write rules), NOT ephemeral
# caches — tracked in git so fresh worktrees and the Test gate receive them with
# no seeding step (#574). The runtime emit routes through writeIfChanged so a
# refresh of identical content never dirties the tree.
# All machine-local/transient runtime state now lives under .tmp/aitm/ (#573),
# already ignored wholesale via the `.tmp/` rule above: state, queue, fleet,
# cache, orchestrator.lock, locks/, sessions/, gates/, and per-app (app/claude,
# app/codex) trees. No per-file rules needed here.
# Per-host install artifacts (Claude Code / Codex / Agents — regenerable from skill/)
.claude/
.agents/

.worktrees/
